# i18n 多语言支持实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Bobcorn 引入完整的 i18n 多语言支持（中英双语 + 社区可扩展），覆盖 renderer UI 和主进程菜单。

**Architecture:** 使用 i18next + react-i18next，主进程和渲染进程各自初始化独立 i18next 实例，加载同一份 JSON 翻译文件。语言切换通过 IPC 同步，偏好存 localStorage。

**Tech Stack:** i18next, react-i18next, Electron IPC

---

## File Structure

### 新建文件

| 文件 | 职责 |
|------|------|
| `src/locales/zh-CN.json` | 中文翻译（~170 keys） |
| `src/locales/en.json` | 英文翻译 |
| `src/locales/index.ts` | 导出 resources + supportedLanguages |
| `src/renderer/i18n.ts` | Renderer 侧 i18next 初始化 |
| `src/main/i18n.ts` | Main process 侧 i18next 初始化 |

### 修改文件

| 文件 | 变更 |
|------|------|
| `package.json` | 添加 i18next + react-i18next 依赖 |
| `src/renderer/entry.ts:9` | 添加 `import './i18n'` |
| `src/main/index.ts` | 添加 i18n import + language-changed IPC handler |
| `src/main/menu.ts` | 重构为 `buildMenu()` 函数 + i18n.t() |
| `src/preload/index.ts:146` | 添加 languageChanged + onLanguageRequest IPC |
| `src/renderer/components/SideMenu/FileMenuBar.tsx` | 用 t() 替换中文字符串 |
| `src/renderer/components/SideMenu/ResourceNav.tsx` | 用 t() 替换中文字符串 |
| `src/renderer/components/SideMenu/ImportExportBar.tsx` | 用 t() 替换中文字符串 |
| `src/renderer/components/SideMenu/GroupDialogs.tsx` | 用 t() 替换中文字符串 |
| `src/renderer/components/SideMenu/PrefixDialog.tsx` | 用 t() 替换中文字符串 + 语言选择 UI |
| `src/renderer/components/SideMenu/ExportDialog.tsx` | 用 t() 替换中文字符串 |
| `src/renderer/components/SideEditor/index.tsx` | 用 t() 替换中文字符串 |
| `src/renderer/components/BatchPanel/index.tsx` | 用 t() 替换中文字符串 |
| `src/renderer/components/IconToolbar/index.tsx` | 用 t() 替换中文字符串 |
| `src/renderer/components/SplashScreen/index.tsx` | 用 t() 替换中文字符串 |
| `src/renderer/containers/MainContainer/index.tsx` | 用 t() 替换中文字符串 |
| `src/renderer/components/IconGridLocal/index.tsx` | 用 t() 替换中文字符串 |
| `docs/CONVENTIONS.md` | 添加 i18n 规范 |
| `AGENTS.md` | 添加 i18n 约定 |
| `README.md` | 添加翻译贡献指南 |

---

## Task 1: 安装依赖 + 创建翻译基础设施

**Files:**
- Modify: `package.json`
- Create: `src/locales/zh-CN.json`
- Create: `src/locales/en.json`
- Create: `src/locales/index.ts`

- [ ] **Step 1: 安装 i18next 和 react-i18next**

```bash
FNM="/c/Users/mail/AppData/Local/Microsoft/WinGet/Packages/Schniz.fnm_Microsoft.Winget.Source_8wekyb3d8bbwe/fnm.exe"
eval "$("$FNM" env --shell bash)" && "$FNM" use 18
cd /d/Code/bobcorn && npm install i18next react-i18next --legacy-peer-deps
```

- [ ] **Step 2: 创建中文翻译文件 `src/locales/zh-CN.json`**

包含所有从组件中提取的中文字符串，按命名空间分组。Key 使用扁平点分格式。

```json
{
  "menu.file": "文件",
  "menu.file.new": "新建项目",
  "menu.file.newDesc": "创建空白项目",
  "menu.file.open": "打开",
  "menu.file.openDesc": "打开 .icp 项目文件",
  "menu.file.save": "保存",
  "menu.file.saveDesc": "保存当前项目",
  "menu.file.saveAs": "另存为",
  "menu.file.saveAsDesc": "保存到新路径",
  "menu.file.importIcons": "导入图标",
  "menu.file.importIconsDesc": "添加 SVG 图标到当前项目",
  "menu.file.exportFonts": "导出字体",
  "menu.file.exportFontsDesc": "导出图标字体文件供网页使用",
  "menu.file.closeProject": "关闭项目",
  "menu.file.closeProjectDesc": "返回欢迎页",
  "menu.file.settings": "设置",
  "menu.file.settingsDesc": "字体前缀与项目配置",
  "menu.file.close": "关闭",
  "menu.edit": "编辑",
  "menu.edit.undo": "撤销",
  "menu.edit.redo": "重做",
  "menu.edit.cut": "剪切",
  "menu.edit.copy": "复制",
  "menu.edit.paste": "粘贴",
  "menu.edit.selectAll": "全选",
  "menu.view": "视图",
  "menu.view.reload": "重新加载",
  "menu.view.toggleFullScreen": "切换全屏",
  "menu.view.toggleDevTools": "切换开发者工具",
  "menu.window": "窗口",
  "menu.window.minimize": "最小化",
  "menu.window.close": "关闭",
  "menu.window.bringAllToFront": "全部置顶",
  "menu.help": "帮助",
  "menu.help.learnMore": "了解更多",
  "menu.help.docs": "文档",
  "menu.help.community": "社区讨论",
  "menu.help.searchIssues": "搜索问题",
  "menu.about": "关于 Bobcorn",
  "menu.services": "服务",
  "menu.hide": "隐藏 Bobcorn",
  "menu.hideOthers": "隐藏其他应用",
  "menu.showAll": "显示全部",
  "menu.quit": "退出",
  "menu.inspectElement": "检查元素",

  "nav.resource": "资源",
  "nav.all": "全部",
  "nav.favorites": "收藏",
  "nav.recentlyUpdated": "最近更新",
  "nav.ungrouped": "未分组",
  "nav.trash": "回收站",

  "splash.welcome": "欢迎使用",
  "splash.newProject": "启动新项目",
  "splash.openProject": "打开项目文件",
  "splash.history": "历史记录",
  "splash.clearAll": "清除全部",
  "splash.noHistory": "没有历史记录",
  "splash.removeRecord": "移除记录",

  "group.add": "添加分组",
  "group.edit": "编辑分组",
  "group.delete": "删除分组",
  "group.name": "分组名称",
  "group.descriptionOptional": "描述（可选）",
  "group.descriptionPlaceholder": "为分组添加一段描述...",
  "group.nameRequired": "请输入一个分组名称",
  "group.nameEmpty": "分组名称不能为空",
  "group.addInputTitle": "请输入要创建的分组名",
  "group.editInputTitle": "分组名称",
  "group.addSuccess": "添加分组成功",
  "group.updateSuccess": "分组已更新",

  "editor.name": "名称",
  "editor.namePlaceholder": "在界面上显示的名称",
  "editor.code": "字码",
  "editor.codePlaceholder": "十六进制, 从E000到F8FF",
  "editor.nameEmpty": "图标名称不能为空",
  "editor.codeEmpty": "图标字码不能为空",
  "editor.codeDuplicate": "图标字码已被占用",
  "editor.codeOutOfRange": "图标字码超出 E000-F8FF",
  "editor.nameChanged": "图标名称已修改",
  "editor.codeChanged": "图标字码已修改",
  "editor.dataUpdated": "图标数据已更新",
  "editor.exported": "图标已导出",
  "editor.allExported": "{{count}} 个图标已导出",
  "editor.exportError": "导出错误: {{error}}",
  "editor.selectSvgFile": "选择一个SVG图标文件",
  "editor.svgFileFilter": "SVG图标文件",
  "editor.exportIcon": "导出图标",
  "editor.exportAllIcons": "导出所有图标",
  "editor.recycleTitle": "回收图标",
  "editor.recycleContent": "图标将会被移动到回收站, 并在导出后的预览页面内不可见, 但仍可被使用. 请在确保图标没有被引用后将其从回收站内删除",
  "editor.recycled": "所选的图标已回收",
  "editor.deleteTitle": "删除图标",
  "editor.deleteContent": "当图标没有在项目中被引用时, 将其删除以释放图标字码",
  "editor.deleted": "所选的图标已被删除",
  "editor.copyToGroup": "选择要复制到的目标分组",
  "editor.moveToGroup": "选择要移动到的目标分组",
  "editor.copiedToGroup": "所选的图标已复制到目标分组",
  "editor.movedToGroup": "所选的图标已移动到目标分组",
  "editor.duplicateHint": "新生成的图标将会拥有一个不同的图标字码",
  "editor.basicInfo": "基本信息",
  "editor.group": "所属分组",
  "editor.originalSize": "原始大小",
  "editor.fileFormat": "文件格式",
  "editor.createDate": "添加日期",
  "editor.updateDate": "修改日期",
  "editor.color": "颜色",
  "editor.eyeDropper": "从屏幕取色",
  "editor.resetColors": "恢复初始颜色",
  "editor.advancedOps": "高级操作",
  "editor.export": "导出",
  "editor.replace": "替换",
  "editor.recycle": "回收",
  "editor.delete": "删除",
  "editor.copy": "复制",
  "editor.move": "移动",
  "editor.selectIconHint": "请选择一个图标",
  "editor.editPropsHint": "可在此编辑其属性",

  "batch.selected": "已选中 {{count}} 个图标",
  "batch.moveTo": "移动到分组",
  "batch.copyTo": "复制到分组",
  "batch.moveToLabel": "移动到",
  "batch.copyToLabel": "复制到",
  "batch.favorite": "收藏",
  "batch.unfavorite": "取消收藏",
  "batch.delete": "删除",
  "batch.exportSvg": "导出 SVG",
  "batch.unifyColor": "统一颜色",
  "batch.applyToAll": "应用到全部",
  "batch.deleteTitle": "批量删除",
  "batch.deleteConfirm": "确定要将 {{count}} 个图标移入回收站吗？",
  "batch.deleteOk": "删除",
  "batch.moved": "已移动 {{count}} 个图标",
  "batch.copied": "已复制 {{count}} 个图标",
  "batch.deleted": "已删除 {{count}} 个图标",
  "batch.exportedSvg": "已导出 {{count}} 个图标",
  "batch.favorited": "已收藏 {{count}} 个图标",
  "batch.unfavorited": "已取消收藏 {{count}} 个图标",
  "batch.colorApplied": "已统一 {{count}} 个图标颜色",
  "batch.selectExportDir": "选择导出目录",
  "batch.cancelSelection": "取消选择",

  "export.title": "导出图标字体",
  "export.done": "导出完成",
  "export.failed": "导出失败",
  "export.exporting": "正在导出...",
  "export.description": "导出图标字体能让您在网页中以图标字码或关联类名的方式直接引用图标。",
  "export.groups": "导出分组",
  "export.groupsAll": "全部 ({{groups}} 个分组，{{icons}} 个图标)",
  "export.groupsPartial": "{{groups}} 个分组，{{icons}} 个图标",
  "export.selectAll": "全选",
  "export.icpMigrationHint": "项目文件 (.icp) 现在通过「保存」({{shortcut}}) 管理。如仍需在导出中包含，请勾选下方选项。",
  "export.requiredFormats": "必选格式",
  "export.optionalFormats": "可选格式",
  "export.includeIcp": "包含 .icp 项目文件",
  "export.includeIcpDesc": "在导出数据中附带项目文件，方便后续编辑",
  "export.zip": "自动打包 (ZIP)",
  "export.zipDesc": "勾选后导出文件自动压缩为 ZIP 包",
  "export.location": "导出位置",
  "export.noDir": "未选择目录",
  "export.browse": "选择…",
  "export.targetPath": "文件将导出至：",
  "export.selectLocationTitle": "选择导出位置",
  "export.selectLocationWarning": "请先选择导出位置",
  "export.noIconsWarning": "当前项目没有任何图标可供导出",
  "export.exportBtn": "导出图标字体",
  "export.previewPage": "预览页面",
  "export.projectFile": "项目文件",
  "export.openDir": "打开目录",
  "export.icpEditHint": "下次需要继续编辑图标，请打开 .icp 文件",
  "export.progress.preparing": "准备导出 {{count}} 个图标...",
  "export.progress.css": "生成 CSS 样式表...",
  "export.progress.jsSymbol": "生成 JS Symbol 引用...",
  "export.progress.svg": "生成 SVG 字体 ({{count}} glyphs)...",
  "export.progress.svgDone": "  SVG 字体生成完成 ({{count}} glyphs)",
  "export.progress.svgFailed": "SVG 字体生成失败",
  "export.progress.ttf": "转换 TTF 字体...",
  "export.progress.woff2": "转换 WOFF2 字体...",
  "export.progress.woff": "转换 WOFF 字体...",
  "export.progress.eot": "转换 EOT 字体...",
  "export.progress.html": "生成 HTML 演示页面...",
  "export.progress.icp": "导出项目文件...",
  "export.progress.writing": "写入文件...",
  "export.progress.zipping": "打包为 ZIP 文件...",
  "export.progress.writeZip": "写入 {{name}}.zip ({{count}} 个文件)",
  "export.progress.writeFile": "写入 {{name}}",
  "export.progress.success": "✓ 导出完成！共 {{count}} 个文件{{zip}}",
  "export.progress.checkOutline": "请确保路径已全部转换为轮廓",
  "export.progress.failed": "✗ 导出失败: {{error}}",

  "toolbar.iconName": "图标名称",
  "toolbar.iconCode": "图标字码",
  "toolbar.sortByAddTime": "按添加时间",
  "toolbar.sortByEditTime": "按修改时间",
  "toolbar.sortByName": "按名称",
  "toolbar.sortByCode": "按字码",
  "toolbar.sortBySize": "按大小",
  "toolbar.ascending": "升序",
  "toolbar.descending": "降序",
  "toolbar.batch": "批量",
  "toolbar.batchMode": "批量选择模式",
  "toolbar.selectAll": "全选",
  "toolbar.invertSelection": "反选",
  "toolbar.cancelAll": "取消全选",
  "toolbar.cancel": "取消",
  "toolbar.search": "搜索图标名称或字码",

  "file.unsavedTitle": "未保存的更改",
  "file.unsavedContent": "当前项目有未保存的更改，是否继续？未保存的更改将会丢失。",
  "file.unsavedCloseContent": "当前项目有未保存的更改。",
  "file.continue": "继续",
  "file.saveAndClose": "保存并关闭",
  "file.saveDialogTitle": "保存项目文件",
  "file.opened": "项目已打开",
  "file.saved": "项目已保存",
  "file.saveFailed": "保存失败: {{error}}",
  "file.noChanges": "没有需要保存的更改",

  "prefix.title": "修改图标字体前缀",
  "prefix.confirmTitle": "确认修改图标字体前缀？",
  "prefix.confirmContent": "此操作将影响所有已引用的图标前缀，修改后需要同步更新代码中的相关引用。",
  "prefix.confirmOk": "确认修改",
  "prefix.success": "图标字体前缀已修改",
  "prefix.emptyError": "图标字体前缀不能为空或包含空格",
  "prefix.warningTitle": "请务必当心",
  "prefix.warningLine1": "一旦你修改了图标字体前缀，被引用的所有图标的相应前缀都会被变更",
  "prefix.warningLine2": "与此同时，您必须同步修改代码中所有引用到该图标的相关代码",
  "prefix.placeholder": "前缀名称",
  "prefix.inputTitle": "请输入新的前缀",

  "import.success": "已成功导入 {{count}} 个图标",
  "import.formatError": "图标格式不相符, 仅支持导入 SVG 格式图标",
  "import.incompatibleTitle": "发现了准备导入的图标中存在不相容的格式",
  "import.incompatibleContent": "所选的图片中包含了非 SVG 格式的图标, 是否仅导入所选文件中的 SVG 格式图标? 非 SVG 格式的文件将不会被导入。",
  "import.importCompatible": "仅导入相容的文件",
  "import.partialSuccess": "已导入了 {{total}} 个图标中的 {{count}} 个",
  "import.cancelled": "导入已取消",

  "emptyState.noIcons": "还没有图标",
  "emptyState.dragToAdd": "直接拖拽图标到此处可添加图标",
  "emptyState.allCategorized": "图标都已经妥善分类了",
  "emptyState.uncategorizedHint": "当新加入的图标未分类时, 将出现在此处",
  "emptyState.noUpdates": "还没有更新过的图标",
  "emptyState.noFavorites": "还没有收藏的图标",
  "emptyState.favoriteHint": "将鼠标悬停在图标上, 点击星标即可收藏",
  "emptyState.trashEmpty": "回收站很干净",
  "emptyState.trashHint": "当图标被回收后, 将会出现在此处",
  "emptyState.emptyGroup": "这个分组没有图标",
  "emptyState.dragToGroup": "拖拽到此处将图标添加到该分组",

  "settings.title": "设置",
  "settings.language": "语言 / Language",
  "settings.languageDesc": "选择界面显示语言",
  "settings.followSystem": "跟随系统",

  "common.cancel": "取消",
  "common.confirm": "确认",
  "common.save": "保存",
  "common.delete": "删除",
  "common.close": "关闭"
}
```

- [ ] **Step 3: 创建英文翻译文件 `src/locales/en.json`**

结构与 zh-CN.json 完全一致，翻译为英文。

```json
{
  "menu.file": "File",
  "menu.file.new": "New Project",
  "menu.file.newDesc": "Create a blank project",
  "menu.file.open": "Open",
  "menu.file.openDesc": "Open .icp project file",
  "menu.file.save": "Save",
  "menu.file.saveDesc": "Save current project",
  "menu.file.saveAs": "Save As",
  "menu.file.saveAsDesc": "Save to a new path",
  "menu.file.importIcons": "Import Icons",
  "menu.file.importIconsDesc": "Add SVG icons to current project",
  "menu.file.exportFonts": "Export Fonts",
  "menu.file.exportFontsDesc": "Export icon fonts for web use",
  "menu.file.closeProject": "Close Project",
  "menu.file.closeProjectDesc": "Return to welcome page",
  "menu.file.settings": "Settings",
  "menu.file.settingsDesc": "Font prefix & project settings",
  "menu.file.close": "Close",
  "menu.edit": "Edit",
  "menu.edit.undo": "Undo",
  "menu.edit.redo": "Redo",
  "menu.edit.cut": "Cut",
  "menu.edit.copy": "Copy",
  "menu.edit.paste": "Paste",
  "menu.edit.selectAll": "Select All",
  "menu.view": "View",
  "menu.view.reload": "Reload",
  "menu.view.toggleFullScreen": "Toggle Full Screen",
  "menu.view.toggleDevTools": "Toggle Developer Tools",
  "menu.window": "Window",
  "menu.window.minimize": "Minimize",
  "menu.window.close": "Close",
  "menu.window.bringAllToFront": "Bring All to Front",
  "menu.help": "Help",
  "menu.help.learnMore": "Learn More",
  "menu.help.docs": "Documentation",
  "menu.help.community": "Community Discussions",
  "menu.help.searchIssues": "Search Issues",
  "menu.about": "About Bobcorn",
  "menu.services": "Services",
  "menu.hide": "Hide Bobcorn",
  "menu.hideOthers": "Hide Others",
  "menu.showAll": "Show All",
  "menu.quit": "Quit",
  "menu.inspectElement": "Inspect Element",

  "nav.resource": "Resources",
  "nav.all": "All",
  "nav.favorites": "Favorites",
  "nav.recentlyUpdated": "Recently Updated",
  "nav.ungrouped": "Ungrouped",
  "nav.trash": "Trash",

  "splash.welcome": "Welcome",
  "splash.newProject": "New Project",
  "splash.openProject": "Open Project File",
  "splash.history": "History",
  "splash.clearAll": "Clear All",
  "splash.noHistory": "No history",
  "splash.removeRecord": "Remove record",

  "group.add": "Add Group",
  "group.edit": "Edit Group",
  "group.delete": "Delete Group",
  "group.name": "Group Name",
  "group.descriptionOptional": "Description (optional)",
  "group.descriptionPlaceholder": "Add a description for this group...",
  "group.nameRequired": "Please enter a group name",
  "group.nameEmpty": "Group name cannot be empty",
  "group.addInputTitle": "Enter the group name to create",
  "group.editInputTitle": "Group Name",
  "group.addSuccess": "Group added successfully",
  "group.updateSuccess": "Group updated",

  "editor.name": "Name",
  "editor.namePlaceholder": "Display name in the interface",
  "editor.code": "Code",
  "editor.codePlaceholder": "Hexadecimal, from E000 to F8FF",
  "editor.nameEmpty": "Icon name cannot be empty",
  "editor.codeEmpty": "Icon code cannot be empty",
  "editor.codeDuplicate": "Icon code is already in use",
  "editor.codeOutOfRange": "Icon code out of range E000-F8FF",
  "editor.nameChanged": "Icon name updated",
  "editor.codeChanged": "Icon code updated",
  "editor.dataUpdated": "Icon data updated",
  "editor.exported": "Icon exported",
  "editor.allExported": "{{count}} icons exported",
  "editor.exportError": "Export error: {{error}}",
  "editor.selectSvgFile": "Select an SVG icon file",
  "editor.svgFileFilter": "SVG icon files",
  "editor.exportIcon": "Export Icon",
  "editor.exportAllIcons": "Export All Icons",
  "editor.recycleTitle": "Recycle Icon",
  "editor.recycleContent": "The icon will be moved to trash and will not be visible in the exported preview page, but can still be used. Please delete it from the trash after confirming it is no longer referenced.",
  "editor.recycled": "Icon has been recycled",
  "editor.deleteTitle": "Delete Icon",
  "editor.deleteContent": "Delete the icon to free up its code point when it is no longer referenced in your project",
  "editor.deleted": "Icon has been deleted",
  "editor.copyToGroup": "Select target group to copy to",
  "editor.moveToGroup": "Select target group to move to",
  "editor.copiedToGroup": "Icon copied to target group",
  "editor.movedToGroup": "Icon moved to target group",
  "editor.duplicateHint": "The duplicated icon will have a different code point",
  "editor.basicInfo": "Basic Info",
  "editor.group": "Group",
  "editor.originalSize": "Original Size",
  "editor.fileFormat": "File Format",
  "editor.createDate": "Date Added",
  "editor.updateDate": "Date Modified",
  "editor.color": "Color",
  "editor.eyeDropper": "Pick color from screen",
  "editor.resetColors": "Reset to original colors",
  "editor.advancedOps": "Advanced",
  "editor.export": "Export",
  "editor.replace": "Replace",
  "editor.recycle": "Recycle",
  "editor.delete": "Delete",
  "editor.copy": "Copy",
  "editor.move": "Move",
  "editor.selectIconHint": "Select an icon",
  "editor.editPropsHint": "to edit its properties here",

  "batch.selected": "{{count}} icons selected",
  "batch.moveTo": "Move to Group",
  "batch.copyTo": "Copy to Group",
  "batch.moveToLabel": "Move to",
  "batch.copyToLabel": "Copy to",
  "batch.favorite": "Favorite",
  "batch.unfavorite": "Unfavorite",
  "batch.delete": "Delete",
  "batch.exportSvg": "Export SVG",
  "batch.unifyColor": "Unify Color",
  "batch.applyToAll": "Apply to All",
  "batch.deleteTitle": "Batch Delete",
  "batch.deleteConfirm": "Are you sure you want to move {{count}} icons to the trash?",
  "batch.deleteOk": "Delete",
  "batch.moved": "Moved {{count}} icons",
  "batch.copied": "Copied {{count}} icons",
  "batch.deleted": "Deleted {{count}} icons",
  "batch.exportedSvg": "Exported {{count}} icons",
  "batch.favorited": "Added {{count}} icons to favorites",
  "batch.unfavorited": "Removed {{count}} icons from favorites",
  "batch.colorApplied": "Applied color to {{count}} icons",
  "batch.selectExportDir": "Select export directory",
  "batch.cancelSelection": "Cancel Selection",

  "export.title": "Export Icon Font",
  "export.done": "Export Complete",
  "export.failed": "Export Failed",
  "export.exporting": "Exporting...",
  "export.description": "Export icon fonts so you can reference icons by code point or class name in web pages.",
  "export.groups": "Export Groups",
  "export.groupsAll": "All ({{groups}} groups, {{icons}} icons)",
  "export.groupsPartial": "{{groups}} groups, {{icons}} icons",
  "export.selectAll": "Select All",
  "export.icpMigrationHint": "Project files (.icp) are now managed via Save ({{shortcut}}). To include in export, check the option below.",
  "export.requiredFormats": "Required Formats",
  "export.optionalFormats": "Optional Formats",
  "export.includeIcp": "Include .icp project file",
  "export.includeIcpDesc": "Attach project file in export for future editing",
  "export.zip": "Auto-pack (ZIP)",
  "export.zipDesc": "Compress exported files into a ZIP archive",
  "export.location": "Export Location",
  "export.noDir": "No directory selected",
  "export.browse": "Browse…",
  "export.targetPath": "Files will be exported to:",
  "export.selectLocationTitle": "Select Export Location",
  "export.selectLocationWarning": "Please select an export location first",
  "export.noIconsWarning": "No icons available for export",
  "export.exportBtn": "Export Icon Font",
  "export.previewPage": "Preview Page",
  "export.projectFile": "Project File",
  "export.openDir": "Open Directory",
  "export.icpEditHint": "To continue editing icons later, open the .icp file",
  "export.progress.preparing": "Preparing to export {{count}} icons...",
  "export.progress.css": "Generating CSS stylesheet...",
  "export.progress.jsSymbol": "Generating JS Symbol references...",
  "export.progress.svg": "Generating SVG font ({{count}} glyphs)...",
  "export.progress.svgDone": "  SVG font complete ({{count}} glyphs)",
  "export.progress.svgFailed": "SVG font generation failed",
  "export.progress.ttf": "Converting TTF font...",
  "export.progress.woff2": "Converting WOFF2 font...",
  "export.progress.woff": "Converting WOFF font...",
  "export.progress.eot": "Converting EOT font...",
  "export.progress.html": "Generating HTML demo page...",
  "export.progress.icp": "Exporting project file...",
  "export.progress.writing": "Writing files...",
  "export.progress.zipping": "Packing ZIP file...",
  "export.progress.writeZip": "Writing {{name}}.zip ({{count}} files)",
  "export.progress.writeFile": "Writing {{name}}",
  "export.progress.success": "✓ Export complete! {{count}} files{{zip}}",
  "export.progress.checkOutline": "Please ensure all paths have been converted to outlines",
  "export.progress.failed": "✗ Export failed: {{error}}",

  "toolbar.iconName": "Icon Name",
  "toolbar.iconCode": "Icon Code",
  "toolbar.sortByAddTime": "By Date Added",
  "toolbar.sortByEditTime": "By Date Modified",
  "toolbar.sortByName": "By Name",
  "toolbar.sortByCode": "By Code",
  "toolbar.sortBySize": "By Size",
  "toolbar.ascending": "Ascending",
  "toolbar.descending": "Descending",
  "toolbar.batch": "Batch",
  "toolbar.batchMode": "Batch selection mode",
  "toolbar.selectAll": "Select All",
  "toolbar.invertSelection": "Invert",
  "toolbar.cancelAll": "Deselect All",
  "toolbar.cancel": "Cancel",
  "toolbar.search": "Search icon name or code",

  "file.unsavedTitle": "Unsaved Changes",
  "file.unsavedContent": "The current project has unsaved changes. Continue? Unsaved changes will be lost.",
  "file.unsavedCloseContent": "The current project has unsaved changes.",
  "file.continue": "Continue",
  "file.saveAndClose": "Save & Close",
  "file.saveDialogTitle": "Save Project File",
  "file.opened": "Project opened",
  "file.saved": "Project saved",
  "file.saveFailed": "Save failed: {{error}}",
  "file.noChanges": "No changes to save",

  "prefix.title": "Change Icon Font Prefix",
  "prefix.confirmTitle": "Confirm prefix change?",
  "prefix.confirmContent": "This will affect all referenced icon prefixes. You will need to update all related code references.",
  "prefix.confirmOk": "Confirm Change",
  "prefix.success": "Icon font prefix updated",
  "prefix.emptyError": "Prefix cannot be empty or contain spaces",
  "prefix.warningTitle": "Please be careful",
  "prefix.warningLine1": "Once you change the icon font prefix, all referenced icons will have their prefix changed",
  "prefix.warningLine2": "You must also update all code references to these icons",
  "prefix.placeholder": "Prefix name",
  "prefix.inputTitle": "Enter the new prefix",

  "import.success": "Successfully imported {{count}} icons",
  "import.formatError": "Incompatible format. Only SVG icons are supported.",
  "import.incompatibleTitle": "Incompatible formats detected in selected icons",
  "import.incompatibleContent": "Some selected files are not SVG format. Import only the SVG files? Non-SVG files will be skipped.",
  "import.importCompatible": "Import compatible files only",
  "import.partialSuccess": "Imported {{count}} of {{total}} icons",
  "import.cancelled": "Import cancelled",

  "emptyState.noIcons": "No icons yet",
  "emptyState.dragToAdd": "Drag and drop icons here to add them",
  "emptyState.allCategorized": "All icons are categorized",
  "emptyState.uncategorizedHint": "Uncategorized icons will appear here",
  "emptyState.noUpdates": "No recently updated icons",
  "emptyState.noFavorites": "No favorite icons yet",
  "emptyState.favoriteHint": "Hover over an icon and click the star to add to favorites",
  "emptyState.trashEmpty": "Trash is empty",
  "emptyState.trashHint": "Recycled icons will appear here",
  "emptyState.emptyGroup": "This group has no icons",
  "emptyState.dragToGroup": "Drag icons here to add to this group",

  "settings.title": "Settings",
  "settings.language": "Language",
  "settings.languageDesc": "Choose display language",
  "settings.followSystem": "Follow System",

  "common.cancel": "Cancel",
  "common.confirm": "Confirm",
  "common.save": "Save",
  "common.delete": "Delete",
  "common.close": "Close"
}
```

- [ ] **Step 4: 创建 `src/locales/index.ts`**

```typescript
import zhCN from './zh-CN.json';
import en from './en.json';

export const resources = {
  'zh-CN': { translation: zhCN },
  en: { translation: en },
};

export const supportedLanguages = [
  { code: 'zh-CN', label: '简体中文' },
  { code: 'en', label: 'English' },
];
```

- [ ] **Step 5: 构建验证**

```bash
npx electron-vite build
```

Expected: build 成功，无 import 错误。

- [ ] **Step 6: Commit**

```bash
git add src/locales/ package.json package-lock.json
git commit -m "feat(i18n): add i18next dependencies and translation files (zh-CN + en)"
```

---

## Task 2: Renderer 侧 i18n 初始化

**Files:**
- Create: `src/renderer/i18n.ts`
- Modify: `src/renderer/entry.ts:9`

- [ ] **Step 1: 创建 `src/renderer/i18n.ts`**

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import { resources } from '../locales';

const savedLng = localStorage.getItem('language');

i18n.use(initReactI18next).init({
  resources,
  lng: savedLng || undefined,
  fallbackLng: 'zh-CN',
  interpolation: { escapeValue: false },
});

export default i18n;
```

- [ ] **Step 2: 在 entry.ts 中导入 i18n**

在 `src/renderer/entry.ts` 的 `// Styles` 注释之前添加：

```typescript
// i18n (must initialize before React mounts)
import './i18n';
```

即在 line 7 (`// Styles`) 之前插入。

- [ ] **Step 3: 构建验证**

```bash
npx electron-vite build
```

Expected: build 成功。

- [ ] **Step 4: Commit**

```bash
git add src/renderer/i18n.ts src/renderer/entry.ts
git commit -m "feat(i18n): initialize i18next in renderer process"
```

---

## Task 3: 主进程 i18n + IPC 同步 + 菜单重构

**Files:**
- Create: `src/main/i18n.ts`
- Modify: `src/main/menu.ts` (全文重构)
- Modify: `src/main/index.ts` (添加 IPC handler + 语言同步)
- Modify: `src/preload/index.ts` (添加 language IPC)

- [ ] **Step 1: 创建 `src/main/i18n.ts`**

```typescript
import i18n from 'i18next';
import { app } from 'electron';
import { resources } from '../locales';

const mainI18n = i18n.createInstance();
mainI18n.init({
  resources,
  lng: app.getLocale().startsWith('zh') ? 'zh-CN' : app.getLocale(),
  fallbackLng: 'zh-CN',
  interpolation: { escapeValue: false },
});

export default mainI18n;
```

- [ ] **Step 2: 重构 `src/main/menu.ts`**

将 MenuBuilder class 改为使用 `i18n.t()` 获取菜单标签。核心变更：

```typescript
import type { MenuItemConstructorOptions } from 'electron';
import { app, Menu, shell, BrowserWindow } from 'electron';
import i18n from './i18n';

export default class MenuBuilder {
  private mainWindow: BrowserWindow;

  constructor(mainWindow: BrowserWindow) {
    this.mainWindow = mainWindow;
  }

  buildMenu(): Menu {
    if (process.env.NODE_ENV === 'development' || process.env.DEBUG_PROD === 'true') {
      this.setupDevelopmentEnvironment();
    }

    const template =
      process.platform === 'darwin' ? this.buildDarwinTemplate() : this.buildDefaultTemplate();

    const menu = Menu.buildFromTemplate(template);
    Menu.setApplicationMenu(menu);

    return menu;
  }

  setupDevelopmentEnvironment(): void {
    this.mainWindow.openDevTools();
    this.mainWindow.webContents.on('context-menu', (_e, props) => {
      const { x, y } = props;
      Menu.buildFromTemplate([
        {
          label: i18n.t('menu.inspectElement'),
          click: () => {
            this.mainWindow.inspectElement(x, y);
          },
        },
      ]).popup({ window: this.mainWindow });
    });
  }

  private buildFileSubmenu(isMac: boolean): MenuItemConstructorOptions {
    const t = i18n.t.bind(i18n);
    const mod = isMac ? 'Command' : 'Ctrl';
    return {
      label: isMac ? t('menu.file') : `&${t('menu.file')}`,
      submenu: [
        {
          label: t('menu.file.new'),
          accelerator: `${mod}+N`,
          click: () => { this.mainWindow.webContents.send('menu:new-project'); },
        },
        {
          label: t('menu.file.open') + '...',
          accelerator: `${mod}+O`,
          click: () => { this.mainWindow.webContents.send('menu:open-project'); },
        },
        { type: 'separator' },
        {
          label: t('menu.file.save'),
          accelerator: `${mod}+S`,
          click: () => { this.mainWindow.webContents.send('menu:save'); },
        },
        {
          label: t('menu.file.saveAs') + '...',
          accelerator: `${mod}+Shift+S`,
          click: () => { this.mainWindow.webContents.send('menu:save-as'); },
        },
        { type: 'separator' },
        {
          label: t('menu.file.importIcons') + '...',
          accelerator: `${mod}+I`,
          click: () => { this.mainWindow.webContents.send('menu:import-icons'); },
        },
        {
          label: t('menu.file.exportFonts') + '...',
          accelerator: `${mod}+E`,
          click: () => { this.mainWindow.webContents.send('menu:export-fonts'); },
        },
        ...(isMac
          ? []
          : [
              { type: 'separator' as const },
              {
                label: `&${t('menu.file.close')}`,
                accelerator: `${mod}+W`,
                click: () => { this.mainWindow.close(); },
              },
            ]),
      ],
    };
  }

  buildDarwinTemplate(): MenuItemConstructorOptions[] {
    const t = i18n.t.bind(i18n);
    const subMenuAbout: MenuItemConstructorOptions = {
      label: 'Bobcorn',
      submenu: [
        { label: t('menu.about'), selector: 'orderFrontStandardAboutPanel:' },
        { type: 'separator' },
        { label: t('menu.services'), submenu: [] },
        { type: 'separator' },
        { label: t('menu.hide'), accelerator: 'Command+H', selector: 'hide:' },
        { label: t('menu.hideOthers'), accelerator: 'Command+Shift+H', selector: 'hideOtherApplications:' },
        { label: t('menu.showAll'), selector: 'unhideAllApplications:' },
        { type: 'separator' },
        { label: t('menu.quit'), accelerator: 'Command+Q', click: () => { app.quit(); } },
      ],
    };
    const subMenuEdit: MenuItemConstructorOptions = {
      label: t('menu.edit'),
      submenu: [
        { label: t('menu.edit.undo'), accelerator: 'Command+Z', selector: 'undo:' },
        { label: t('menu.edit.redo'), accelerator: 'Shift+Command+Z', selector: 'redo:' },
        { type: 'separator' },
        { label: t('menu.edit.cut'), accelerator: 'Command+X', selector: 'cut:' },
        { label: t('menu.edit.copy'), accelerator: 'Command+C', selector: 'copy:' },
        { label: t('menu.edit.paste'), accelerator: 'Command+V', selector: 'paste:' },
        { label: t('menu.edit.selectAll'), accelerator: 'Command+A', selector: 'selectAll:' },
      ],
    };
    const subMenuViewDev: MenuItemConstructorOptions = {
      label: t('menu.view'),
      submenu: [
        { label: t('menu.view.reload'), accelerator: 'Command+R', click: () => { this.mainWindow.webContents.reload(); } },
        { label: t('menu.view.toggleFullScreen'), accelerator: 'Ctrl+Command+F', click: () => { this.mainWindow.setFullScreen(!this.mainWindow.isFullScreen()); } },
        { label: t('menu.view.toggleDevTools'), accelerator: 'Alt+Command+I', click: () => { this.mainWindow.toggleDevTools(); } },
      ],
    };
    const subMenuViewProd: MenuItemConstructorOptions = {
      label: t('menu.view'),
      submenu: [
        { label: t('menu.view.toggleFullScreen'), accelerator: 'Ctrl+Command+F', click: () => { this.mainWindow.setFullScreen(!this.mainWindow.isFullScreen()); } },
      ],
    };
    const subMenuWindow: MenuItemConstructorOptions = {
      label: t('menu.window'),
      submenu: [
        { label: t('menu.window.minimize'), accelerator: 'Command+M', selector: 'performMiniaturize:' },
        { label: t('menu.window.close'), accelerator: 'Command+W', selector: 'performClose:' },
        { type: 'separator' },
        { label: t('menu.window.bringAllToFront'), selector: 'arrangeInFront:' },
      ],
    };
    const subMenuHelp: MenuItemConstructorOptions = {
      label: t('menu.help'),
      submenu: [
        { label: t('menu.help.learnMore'), click() { shell.openExternal('http://electron.atom.io'); } },
        { label: t('menu.help.docs'), click() { shell.openExternal('https://github.com/atom/electron/tree/master/docs#readme'); } },
        { label: t('menu.help.community'), click() { shell.openExternal('https://discuss.atom.io/c/electron'); } },
        { label: t('menu.help.searchIssues'), click() { shell.openExternal('https://github.com/atom/electron/issues'); } },
      ],
    };
    const subMenuView = process.env.NODE_ENV === 'development' ? subMenuViewDev : subMenuViewProd;
    return [subMenuAbout, this.buildFileSubmenu(true), subMenuEdit, subMenuView, subMenuWindow, subMenuHelp];
  }

  buildDefaultTemplate(): MenuItemConstructorOptions[] {
    const t = i18n.t.bind(i18n);
    return [
      this.buildFileSubmenu(false),
      {
        label: `&${t('menu.view')}`,
        submenu:
          process.env.NODE_ENV === 'development'
            ? [
                { label: `&${t('menu.view.reload')}`, accelerator: 'Ctrl+R', click: () => { this.mainWindow.webContents.reload(); } },
                { label: `${t('menu.view.toggleFullScreen')}`, accelerator: 'F11', click: () => { this.mainWindow.setFullScreen(!this.mainWindow.isFullScreen()); } },
                { label: `${t('menu.view.toggleDevTools')}`, accelerator: 'Alt+Ctrl+I', click: () => { this.mainWindow.toggleDevTools(); } },
              ]
            : [
                { label: `${t('menu.view.toggleFullScreen')}`, accelerator: 'F11', click: () => { this.mainWindow.setFullScreen(!this.mainWindow.isFullScreen()); } },
              ],
      },
      {
        label: t('menu.help'),
        submenu: [
          { label: t('menu.help.learnMore'), click() { shell.openExternal('http://electron.atom.io'); } },
          { label: t('menu.help.docs'), click() { shell.openExternal('https://github.com/atom/electron/tree/master/docs#readme'); } },
          { label: t('menu.help.community'), click() { shell.openExternal('https://discuss.atom.io/c/electron'); } },
          { label: t('menu.help.searchIssues'), click() { shell.openExternal('https://github.com/atom/electron/issues'); } },
        ],
      },
    ];
  }
}
```

- [ ] **Step 3: 修改 `src/preload/index.ts` — 添加 language IPC**

在 `closeCancelled` 行 (line 145) 之后、contextBridge 的 `});` 结束之前添加：

```typescript
  // ── Language (i18n) ─────────────────────────────────────────────
  languageChanged: (lng: string): void => ipcRenderer.send('language-changed', lng),
  onLanguageRequest: (callback: () => void) => {
    const handler = () => callback();
    ipcRenderer.on('request-language', handler);
    return () => {
      ipcRenderer.removeListener('request-language', handler);
    };
  },
```

- [ ] **Step 4: 修改 `src/main/index.ts` — 添加 language IPC handler + 启动同步**

在现有 IPC handlers 区域（`ipcMain.on('install-update', ...)` 之后）添加：

```typescript
  // ── Language (i18n) ──────────────────────────────────────────────
  ipcMain.on('language-changed', (_event: Electron.IpcMainEvent, lng: string) => {
    const mainI18n = require('./i18n').default;
    mainI18n.changeLanguage(lng);
    // Rebuild menu with new language
    const menuBuilder = new MenuBuilder(mainWindow!);
    menuBuilder.buildMenu();
  });
```

在 `mainWindow.webContents.on('did-finish-load', ...)` 回调中添加语言请求：

```typescript
  mainWindow.webContents.send('request-language');
```

- [ ] **Step 5: 构建验证**

```bash
npx electron-vite build
```

- [ ] **Step 6: Commit**

```bash
git add src/main/i18n.ts src/main/menu.ts src/main/index.ts src/preload/index.ts
git commit -m "feat(i18n): add main process i18n, menu i18n, and IPC language sync"
```

---

## Task 4: 迁移 renderer 组件 — SplashScreen + ResourceNav + ImportExportBar

最简单的 3 个组件先迁移，建立模式。

**Files:**
- Modify: `src/renderer/components/SplashScreen/index.tsx`
- Modify: `src/renderer/components/SideMenu/ResourceNav.tsx`
- Modify: `src/renderer/components/SideMenu/ImportExportBar.tsx`

- [ ] **Step 1: 迁移 SplashScreen**

在 import 区域添加：
```typescript
import { useTranslation } from 'react-i18next';
```

在组件函数体顶部添加：
```typescript
const { t } = useTranslation();
```

替换所有中文字符串（共 ~8 处）：
- line 31, 45: `'项目已打开'` → `t('file.opened')`
- line 129: `"移除记录"` → `t('splash.removeRecord')`
- line 167: `没有历史记录` → `{t('splash.noHistory')}`
- line 185: `欢迎使用` → `{t('splash.welcome')}`
- line 228: `启动新项目` → `{t('splash.newProject')}`
- line 268: `打开项目文件` → `{t('splash.openProject')}`
- line 288: `历史记录` → `{t('splash.history')}`
- line 299: `清除全部` → `{t('splash.clearAll')}`

- [ ] **Step 2: 迁移 ResourceNav**

添加 `import { useTranslation } from 'react-i18next';` 和 `const { t } = useTranslation();`

替换 items 数组中的 label 值：
- line 30: `'全部'` → `t('nav.all')`
- line 31: `'收藏'` → `t('nav.favorites')`
- line 32: `'最近更新'` → `t('nav.recentlyUpdated')`
- line 36: `'未分组'` → `t('nav.ungrouped')`
- line 39: `'回收站'` → `t('nav.trash')`
- line 47: `资源` → `{t('nav.resource')}`

注意：items 数组使用了 `useMemo` 依赖 `groupData`，改为 hook 调用后需要将 `t` 加入 items 的依赖或将 items 移出 useMemo。由于 items 数量很少（5个），直接每次渲染重建即可（去掉 useMemo 包裹，items 目前没有 useMemo）。

- [ ] **Step 3: 迁移 ImportExportBar**

添加 `import { useTranslation } from 'react-i18next';` 和 `const { t } = useTranslation();`

替换：
- line 24: `导入图标` → `{t('menu.file.importIcons')}`
- line 31: `导出` → `{t('editor.export')}`

- [ ] **Step 4: 运行测试**

```bash
npx vitest run
```

Expected: 所有现有测试通过。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/components/SplashScreen/index.tsx src/renderer/components/SideMenu/ResourceNav.tsx src/renderer/components/SideMenu/ImportExportBar.tsx
git commit -m "feat(i18n): migrate SplashScreen, ResourceNav, ImportExportBar to i18n"
```

---

## Task 5: 迁移 GroupDialogs + PrefixDialog（含语言设置 UI）

**Files:**
- Modify: `src/renderer/components/SideMenu/GroupDialogs.tsx`
- Modify: `src/renderer/components/SideMenu/PrefixDialog.tsx`

- [ ] **Step 1: 迁移 GroupDialogs**

添加 `import { useTranslation } from 'react-i18next';` 和 `const { t } = useTranslation();`

替换所有中文字符串（~12 处）：
- line 65: `'添加分组成功'` → `t('group.addSuccess')`
- line 76: `'请输入一个分组名称'` → `t('group.nameRequired')`
- line 83: `'分组已更新'` → `t('group.updateSuccess')`
- line 89: `'分组名称不能为空'` → `t('group.nameEmpty')`
- line 98: `"添加分组"` → `t('group.add')`
- line 101: `取消` → `{t('common.cancel')}`
- line 103: `确认` → `{t('common.confirm')}`
- line 114: `"请输入要创建的分组名"` → `t('group.addInputTitle')`
- line 110, 147: `"分组名称"` → `t('group.name')`
- line 119, 156: `"描述（可选）"` → `t('group.descriptionOptional')`
- line 122, 159: `"为分组添加一段描述..."` → `t('group.descriptionPlaceholder')`
- line 135: `"编辑分组"` → `t('group.edit')`
- line 138: `取消` → `{t('common.cancel')}`
- line 140: `保存` → `{t('common.save')}`
- line 151: `"分组名称"` → `t('group.editInputTitle')`

- [ ] **Step 2: 迁移 PrefixDialog + 添加语言选择 UI**

添加 `import { useTranslation } from 'react-i18next';` 和 `const { t } = useTranslation();`
添加 `import { supportedLanguages } from '../../../locales';`
添加 `import i18n from '../../../i18n';`

替换所有中文字符串（~12 处），使用 `t('prefix.*')` keys。

在 `</div>` (关闭 prefix 内容区) 之后、`</Dialog>` 之前添加语言选择器：

```tsx
{/* 分隔线 */}
<div className="border-t border-border my-3" />

{/* 语言设置 */}
<div>
  <h4 className="text-xs font-semibold text-foreground-muted mb-2">
    {t('settings.language')}
  </h4>
  <div className="flex items-center gap-3">
    <select
      value={localStorage.getItem('language') === null ? '__system__' : i18n.language}
      onChange={(e) => {
        const val = e.target.value;
        if (val === '__system__') {
          localStorage.removeItem('language');
          const sysLng = navigator.language.startsWith('zh') ? 'zh-CN' : navigator.language;
          i18n.changeLanguage(sysLng);
          window.electronAPI.languageChanged(sysLng);
        } else {
          localStorage.setItem('language', val);
          i18n.changeLanguage(val);
          window.electronAPI.languageChanged(val);
        }
      }}
      className="flex-1 px-2 py-1.5 rounded-md border border-border bg-surface text-sm text-foreground focus:border-brand-400 focus:outline-none focus:ring-1 focus:ring-brand-400/30"
    >
      <option value="__system__">{t('settings.followSystem')}</option>
      {supportedLanguages.map((lng) => (
        <option key={lng.code} value={lng.code}>{lng.label}</option>
      ))}
    </select>
  </div>
  <p className="text-xs text-foreground-muted mt-1">{t('settings.languageDesc')}</p>
</div>
```

- [ ] **Step 3: 运行测试**

```bash
npx vitest run
```

- [ ] **Step 4: Commit**

```bash
git add src/renderer/components/SideMenu/GroupDialogs.tsx src/renderer/components/SideMenu/PrefixDialog.tsx
git commit -m "feat(i18n): migrate GroupDialogs, PrefixDialog + add language selector"
```

---

## Task 6: 迁移 FileMenuBar + IconToolbar

**Files:**
- Modify: `src/renderer/components/SideMenu/FileMenuBar.tsx`
- Modify: `src/renderer/components/IconToolbar/index.tsx`

- [ ] **Step 1: 迁移 FileMenuBar**

添加 `import { useTranslation } from 'react-i18next';`

注意：FileMenuBar 使用 `React.memo`，`useTranslation` 在 memo 组件内部直接使用即可。

在函数体顶部添加 `const { t } = useTranslation();`

将 `menuGroups` 常量从模块顶层移入组件内部（因为 `t()` 是 hook），改为 `useMemo`：

```tsx
const menuGroups: FileMenuItem[][] = useMemo(() => [
  [
    { key: 'new-project', icon: <FilePlus2 size={15} />, label: t('menu.file.new'), description: t('menu.file.newDesc'), shortcut: `${mod}N` },
    { key: 'open-project', icon: <FolderOpen size={15} />, label: t('menu.file.open'), description: t('menu.file.openDesc'), shortcut: `${mod}O` },
    { key: 'save', icon: <Save size={15} />, label: t('menu.file.save'), description: t('menu.file.saveDesc'), shortcut: `${mod}S` },
    { key: 'save-as', icon: <SaveAll size={15} />, label: t('menu.file.saveAs'), description: t('menu.file.saveAsDesc'), shortcut: `${mod}⇧S` },
  ],
  [
    { key: 'import-icons', icon: <Import size={15} />, label: t('menu.file.importIcons'), description: t('menu.file.importIconsDesc'), shortcut: `${mod}I` },
    { key: 'export-fonts', icon: <Upload size={15} />, label: t('menu.file.exportFonts'), description: t('menu.file.exportFontsDesc'), shortcut: `${mod}E` },
  ],
  [
    { key: 'close-project', icon: <X size={15} />, label: t('menu.file.closeProject'), description: t('menu.file.closeProjectDesc') },
  ],
  [
    { key: 'settings', icon: <Settings size={15} />, label: t('menu.file.settings'), description: t('menu.file.settingsDesc') },
  ],
], [t]);
```

替换 line 164 的 `文件` → `{t('menu.file')}`。

- [ ] **Step 2: 迁移 IconToolbar**

添加 `import { useTranslation } from 'react-i18next';` 和 `const { t } = useTranslation();`

替换所有中文字符串（~14 处），包括：
- line 116: `图标名称` → `{t('toolbar.iconName')}`
- line 127: `图标字码` → `{t('toolbar.iconCode')}`
- line 142-146: RadioButton 标签 → `t('toolbar.sortBy*')`
- line 149-150: `升序`/`降序` → `t('toolbar.ascending')`/`t('toolbar.descending')`
- line 198: `"批量选择模式"` → `t('toolbar.batchMode')`
- line 201: `批量` → `{t('toolbar.batch')}`
- line 209-210: `"全选"` + `全选` → `t('toolbar.selectAll')`
- line 216-217: `"反选"` + `反选` → `t('toolbar.invertSelection')`
- line 224-225: `"取消全选"` + `取消` → `t('toolbar.cancelAll')` / `t('toolbar.cancel')`
- line 247: `"搜索图标名称或字码"` → `t('toolbar.search')`

- [ ] **Step 3: 运行测试**

```bash
npx vitest run
```

- [ ] **Step 4: Commit**

```bash
git add src/renderer/components/SideMenu/FileMenuBar.tsx src/renderer/components/IconToolbar/index.tsx
git commit -m "feat(i18n): migrate FileMenuBar and IconToolbar to i18n"
```

---

## Task 7: 迁移 SideEditor

最大的单个组件（~50 字符串）。

**Files:**
- Modify: `src/renderer/components/SideEditor/index.tsx`

- [ ] **Step 1: 添加 i18n import + hook**

```typescript
import { useTranslation } from 'react-i18next';
```

在 `SideEditor` 函数体顶部添加 `const { t } = useTranslation();`

- [ ] **Step 2: 替换表单字段字符串**

- line 116: `'图标名称不能为空'` → `t('editor.nameEmpty')`
- line 122: `'图标名称已修改'` → `t('editor.nameChanged')`
- line 128: `'图标名称不能为空'` → `t('editor.nameEmpty')`
- line 143: `'图标字码已被占用'` → `t('editor.codeDuplicate')`
- line 144: `'图标字码超出 E000-F8FF'` → `t('editor.codeOutOfRange')`
- line 149: `'图标字码不能为空'` → `t('editor.codeEmpty')`
- line 156: `'图标字码已修改'` → `t('editor.codeChanged')`
- line 162: `'图标字码不能为空'` → `t('editor.codeEmpty')`

- [ ] **Step 3: 替换对话框 + 操作字符串**

- line 169: `'选择一个SVG图标文件'` → `t('editor.selectSvgFile')`
- line 170: `'SVG图标文件'` → `t('editor.svgFileFilter')`
- line 176: `'图标数据已更新'` → `t('editor.dataUpdated')`
- line 185: `'导出图标'` → `t('editor.exportIcon')`
- line 191: `'图标已导出'` → `t('editor.exported')`
- line 192: `` `导出错误: ${err.message}` `` → `t('editor.exportError', { error: err.message })`
- line 197: `'导出所有图标'` → `t('editor.exportAllIcons')`
- line 213: `` `${icons.length} 个图标已导出` `` → `t('editor.allExported', { count: icons.length })`
- line 215: 同 192
- line 223: `'回收图标'` → `t('editor.recycleTitle')`
- line 224-225: content → `t('editor.recycleContent')`
- line 228: `'所选的图标已回收'` → `t('editor.recycled')`
- line 237: `'删除图标'` → `t('editor.deleteTitle')`
- line 238: content → `t('editor.deleteContent')`
- line 241: `'所选的图标已被删除'` → `t('editor.deleted')`
- line 253: `'选择要复制到的目标分组'` → `t('editor.copyToGroup')`
- line 261: `'选择要移动到的目标分组'` → `t('editor.moveToGroup')`
- line 271: `'所选的图标已复制到目标分组'` → `t('editor.copiedToGroup')`
- line 278: `'所选的图标已移动到目标分组'` → `t('editor.movedToGroup')`

- [ ] **Step 4: 替换 UI 标签字符串**

- line 438: `"在界面上显示的名称"` → `t('editor.namePlaceholder')`
- line 442: `"名称"` → `t('editor.name')`
- line 452: `"十六进制, 从E000到F8FF"` → `t('editor.codePlaceholder')`
- line 456: `"字码"` → `t('editor.code')`
- line 474: `基本信息` → `{t('editor.basicInfo')}`
- line 478-482: `['所属分组', ...]` → `[t('editor.group'), ...]` etc.
- line 508: `颜色` → `{t('editor.color')}`
- line 570: `"从屏幕取色"` → `t('editor.eyeDropper')`
- line 615: `恢复初始颜色` → `{t('editor.resetColors')}`
- line 631: `高级操作` → `{t('editor.advancedOps')}`
- line 634: `导出` → `{t('editor.export')}`
- line 642: `替换` → `{t('editor.replace')}`
- line 651: `删除`/`回收` → `{t('editor.delete')}`/`{t('editor.recycle')}`
- line 661: `复制` → `{t('editor.copy')}`
- line 669: `移动` → `{t('editor.move')}`
- line 683: `请选择一个图标` → `{t('editor.selectIconHint')}`
- line 684: `可在此编辑其属性` → `{t('editor.editPropsHint')}`
- line 694-707: `取消`/`确认` → `{t('common.cancel')}`/`{t('common.confirm')}`
- line 714: `新生成的图标将会拥有一个不同的图标字码` → `{t('editor.duplicateHint')}`

- [ ] **Step 5: 运行测试**

```bash
npx vitest run
```

- [ ] **Step 6: Commit**

```bash
git add src/renderer/components/SideEditor/index.tsx
git commit -m "feat(i18n): migrate SideEditor to i18n (~50 strings)"
```

---

## Task 8: 迁移 BatchPanel + ExportDialog

**Files:**
- Modify: `src/renderer/components/BatchPanel/index.tsx`
- Modify: `src/renderer/components/SideMenu/ExportDialog.tsx`

- [ ] **Step 1: 迁移 BatchPanel**

添加 `import { useTranslation } from 'react-i18next';` 和 `const { t } = useTranslation();`

替换所有中文字符串（~20 处），包括：
- line 51: `` `已移动 ${selectedIds.length} 个图标` `` → `t('batch.moved', { count: selectedIds.length })`
- line 61: 同理 `batch.copied`
- line 68-70: `'批量删除'` → `t('batch.deleteTitle')`, content → `t('batch.deleteConfirm', { count: ... })`, okText → `t('batch.deleteOk')`
- line 75: `batch.deleted`
- line 82: `'选择导出目录'` → `t('batch.selectExportDir')`
- line 95: `batch.exportedSvg`
- line 103-105: `batch.favorited` / `batch.unfavorited`
- line 113: `batch.colorApplied`
- line 171: `` `已选中 ${selectedIds.length} 个图标` `` → `{t('batch.selected', { count: selectedIds.length })}`
- line 207: `'移动到'`/`'复制到'` → `t('batch.moveToLabel')`/`t('batch.copyToLabel')`
- line 228: `取消` → `{t('common.cancel')}`
- line 236: `统一颜色` → `{t('batch.unifyColor')}`
- line 306: `应用到全部` → `{t('batch.applyToAll')}`
- line 310: `取消` → `{t('common.cancel')}`
- line 322-342: button labels → `t('batch.*')`
- line 354: `取消选择` → `{t('batch.cancelSelection')}`

- [ ] **Step 2: 迁移 ExportDialog**

添加 `import { useTranslation } from 'react-i18next';` 和 `const { t } = useTranslation();`
添加 `import { platform } from '../../utils/tools';`（如果还没导入的话，用于快捷键显示）

这是最大的迁移（~50+ 字符串）。替换策略同上，所有中文字符串替换为对应的 `t('export.*')` 调用。

特别注意模板字符串的插值转换，例如：
- `` `准备导出 ${icons.length} 个图标...` `` → `t('export.progress.preparing', { count: icons.length })`
- `` `✓ 导出完成！共 ${files.length} 个文件${zipEnabled ? ' (ZIP)' : ''}` `` → `t('export.progress.success', { count: files.length, zip: zipEnabled ? ' (ZIP)' : '' })`
- line 177: `'未分组'` → `t('nav.ungrouped')`（复用 nav 命名空间的 key）
- line 384-385 中的分组/图标统计文本 → `t('export.groupsAll', { groups, icons })` / `t('export.groupsPartial', ...)`
- line 412: 迁移提示中的 `Ctrl+S` → 根据平台动态: `t('export.icpMigrationHint', { shortcut: platform() === 'darwin' ? '⌘S' : 'Ctrl+S' })`

- [ ] **Step 3: 运行测试**

```bash
npx vitest run
```

- [ ] **Step 4: Commit**

```bash
git add src/renderer/components/BatchPanel/index.tsx src/renderer/components/SideMenu/ExportDialog.tsx
git commit -m "feat(i18n): migrate BatchPanel and ExportDialog to i18n"
```

---

## Task 9: 迁移 MainContainer + IconGridLocal

**Files:**
- Modify: `src/renderer/containers/MainContainer/index.tsx`
- Modify: `src/renderer/components/IconGridLocal/index.tsx`

- [ ] **Step 1: 迁移 MainContainer**

添加 `import { useTranslation } from 'react-i18next';` 和 `const { t } = useTranslation();`

替换中文字符串（~12 处）：
- line 125: `'未保存的更改'` → `t('file.unsavedTitle')`
- line 126: content → `t('file.unsavedContent')`
- line 127: `'继续'` → `t('file.continue')`
- line 144, 154: `'项目已打开'` → `t('file.opened')`
- line 163: `'保存项目文件'` → `t('file.saveDialogTitle')`
- line 183, 209: `'项目已保存'` → `t('file.saved')`
- line 186, 212: `` `保存失败: ${err.message}` `` → `t('file.saveFailed', { error: err.message })`
- line 197: `'没有需要保存的更改'` → `t('file.noChanges')`
- line 229-231: 同 125-127
- line 252-254: 同 125-127
- line 335-337: `'未保存的更改'`, content, `'保存并关闭'` → `t('file.unsavedTitle')`, `t('file.unsavedCloseContent')`, `t('file.saveAndClose')`

同时在语言启动同步的 useEffect 中添加 `onLanguageRequest` 监听：

```tsx
useEffect(() => {
  const cleanup = electronAPI.onLanguageRequest(() => {
    electronAPI.languageChanged(i18n.language);
  });
  return cleanup;
}, []);
```

需要额外导入 `import i18n from '../../i18n';`

- [ ] **Step 2: 迁移 IconGridLocal**

添加 `import { useTranslation } from 'react-i18next';` 和 `const { t } = useTranslation();`

替换中文字符串（~15 处）：
- line 247: `` `已成功导入 ${acceptableIcons.length} 个图标` `` → `t('import.success', { count: acceptableIcons.length })`
- line 252: `'图标格式不相符, 仅支持导入 SVG 格式图标'` → `t('import.formatError')`
- line 257: title → `t('import.incompatibleTitle')`
- line 259: content → `t('import.incompatibleContent')`
- line 260: okText → `t('import.importCompatible')`
- line 264: `` `已导入了 ... 个图标中的 ... 个` `` → `t('import.partialSuccess', { total: ..., count: ... })`
- line 271: `'导入已取消'` → `t('import.cancelled')`
- line 276: 同 247
- line 363-382: 所有 empty state hints → `t('emptyState.*')`
- line 656: `拖拽到此处将图标添加到该分组` → `{t('emptyState.dragToGroup')}`

- [ ] **Step 3: 运行测试**

```bash
npx vitest run
```

- [ ] **Step 4: Commit**

```bash
git add src/renderer/containers/MainContainer/index.tsx src/renderer/components/IconGridLocal/index.tsx
git commit -m "feat(i18n): migrate MainContainer and IconGridLocal to i18n"
```

---

## Task 10: 更新文档 — CONVENTIONS.md + AGENTS.md + README.md

**Files:**
- Modify: `docs/CONVENTIONS.md`
- Modify: `AGENTS.md`
- Modify: `README.md`

- [ ] **Step 1: 在 `docs/CONVENTIONS.md` 末尾添加 i18n 规范**

```markdown
## Internationalization (i18n)

### All User-Facing Strings Must Use i18n

Every user-visible string (UI labels, messages, dialog titles, error text, tooltips, placeholders) **must** use `t()` from `react-i18next`. No hardcoded Chinese or English strings in components.

```jsx
// CORRECT
import { useTranslation } from 'react-i18next';
const { t } = useTranslation();
<Button>{t('common.cancel')}</Button>
message.success(t('batch.moved', { count: ids.length }));

// WRONG — do not hardcode strings
<Button>取消</Button>
message.success(`已移动 ${ids.length} 个图标`);
```

### Translation Key Conventions

- Flat dot-separated namespace keys: `export.progress.css`, `editor.nameEmpty`
- Namespaces match component areas: `menu.*`, `nav.*`, `editor.*`, `batch.*`, `export.*`, `file.*`, `splash.*`, `group.*`, `toolbar.*`, `settings.*`, `common.*`, `import.*`, `emptyState.*`, `prefix.*`
- Interpolation uses `{{variable}}` syntax: `t('batch.moved', { count: 5 })`
- Shared strings use `common.*` namespace: `common.cancel`, `common.confirm`, `common.save`

### Adding New Strings

When adding a new feature or UI element:

1. Add the key + Chinese value to `src/locales/zh-CN.json`
2. Add the key + English value to `src/locales/en.json`
3. Use `t('namespace.key')` in the component

### Main Process Strings

Menu items and main process dialogs use `src/main/i18n.ts`:

```typescript
import i18n from './i18n';
const t = i18n.t.bind(i18n);
label: t('menu.file.save')
```

### Adding a New Language

See Contributing section in README.md.
```

- [ ] **Step 2: 在 `AGENTS.md` 的「关键约定」section 末尾添加 i18n 规则**

在 `## 关键约定` 末尾添加：

```markdown
- **i18n 必须遵守**: 所有用户可见字符串必须使用 `t()` 函数（`react-i18next`），不允许硬编码中文或英文。新增功能时必须同时在 `src/locales/zh-CN.json` 和 `src/locales/en.json` 中添加翻译 key。详见 `docs/CONVENTIONS.md` 的 i18n 章节。
```

- [ ] **Step 3: 在 `README.md` 的 License 之前添加贡献翻译指南**

在 `## License` 之前添加：

```markdown
## Contributing Translations

Bobcorn supports internationalization. To add a new language:

1. Copy `src/locales/zh-CN.json` → `src/locales/<language-code>.json`
2. Translate all values (do not change the keys)
3. Register the language in `src/locales/index.ts`:

```typescript
import xx from './<language-code>.json';

// Add to resources
resources['<language-code>'] = { translation: xx };

// Add to supportedLanguages
supportedLanguages.push({ code: '<language-code>', label: 'Language Name' });
```

4. Submit a pull request

Missing translations will fall back to Chinese (zh-CN).

```

- [ ] **Step 4: Commit**

```bash
git add docs/CONVENTIONS.md AGENTS.md README.md
git commit -m "docs: add i18n conventions, agent rules, and translation contribution guide"
```

---

## Task 11: 最终验证

- [ ] **Step 1: 完整构建**

```bash
npx electron-vite build
```

Expected: 0 errors。

- [ ] **Step 2: 单元测试**

```bash
npx vitest run
```

Expected: 所有现有测试通过。

- [ ] **Step 3: 检查翻译完整性**

手动对比 zh-CN.json 和 en.json 的 key 数量一致：

```bash
cd /d/Code/bobcorn
node -e "const zh = require('./src/locales/zh-CN.json'); const en = require('./src/locales/en.json'); const zhKeys = Object.keys(zh); const enKeys = Object.keys(en); const missing = zhKeys.filter(k => !en[k]); const extra = enKeys.filter(k => !zh[k]); console.log('zh keys:', zhKeys.length, 'en keys:', enKeys.length); if (missing.length) console.log('Missing in en:', missing); if (extra.length) console.log('Extra in en:', extra); if (!missing.length && !extra.length) console.log('All keys match!');"
```

Expected: `All keys match!`

- [ ] **Step 4: Grep 残留中文字符串**

```bash
cd /d/Code/bobcorn
npx grep -r '[\u4e00-\u9fff]' src/renderer/components/ src/renderer/containers/ src/main/menu.ts --include='*.tsx' --include='*.ts' | grep -v '\/\/' | grep -v 'node_modules' | head -30
```

检查是否有遗漏的中文字符串（注释除外）。

- [ ] **Step 5: 最终 Commit（如有修复）**

```bash
git add -A
git commit -m "fix(i18n): address any remaining hardcoded strings"
```
