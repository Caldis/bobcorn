# 测试覆盖缺口分析

> 2026-03-16 | 基于 v1.1.2 (17 E2E + 158 unit tests)

## 覆盖率概览

| 领域 | 覆盖率 | 说明 |
|------|--------|------|
| 核心 CRUD | 90% | 6 个单元测试模块 |
| UI 布局结构 | 85% | 3 个 E2E 套件 |
| 导出功能 | 60% | v1.1.2 新增文件内容验证 (Step 13c) |
| 导入功能 | 40% | SVG + ICP 已测, CP 未测 |
| 搜索/过滤 | 60% | E2E 仅基本搜索 |
| 设置 | 30% | 对话框可见, 无持久化验证 |
| 错误处理 | 5% | 仅收集错误计数 |
| 视觉/UX | 15% | 仅截图 |
| 性能 | 5% | 仅 2949 icons 一个场景 |

## 已覆盖 (v1.1.2)

### Step 13c 导出内容验证 (8 项)
- ✅ CSS @font-face + `.prefix-xxxx:before` class 格式
- ✅ JS `<symbol>` SVG 标签结构
- ✅ SVG `<font>` + `<glyph>` 元素 (2949 glyphs)
- ✅ TTF magic number (00 01 00 00)
- ✅ WOFF magic number (wOFF)
- ✅ WOFF2 magic number (wOF2)
- ✅ HTML 模板结构 + content-visibility
- ✅ ICP SQLite 头部

## 关键缺口 (按优先级)

### P0 — 必须修复

1. **图标编辑操作 E2E**
   - ✗ 编辑图标名称 + 验证持久化
   - ✗ 编辑图标字码 + 验证持久化
   - ✗ 替换 SVG 文件
   - ✗ 回收图标 → 回收站验证
   - ✗ 从回收站恢复

2. **分组操作 E2E**
   - ✗ 分组重命名
   - ✗ 分组删除 (级联删除图标)
   - ✗ 分组拖拽排序

### P1 — 应该修复

3. **导入路径覆盖**
   - ✗ CP (clipboard/JSON) 项目导入
   - ✗ 拖拽导入 SVG
   - ✗ 批量导入 (>10 文件)

4. **设置持久化**
   - ✗ 前缀修改 → 验证导出中的前缀
   - ✗ 图标名称/字码可见性切换 → 验证 UI 效果
   - ✗ 缩放级别 → 验证图标大小

5. **导出 round-trip**
   - ✗ 导出 → 重新导入 .icp → 验证数据一致性

### P2 — 可以做

6. **错误场景**
   - ✗ 损坏的 SVG 文件导入
   - ✗ 超大 SVG (>10MB) 处理
   - ✗ 重复图标名称

7. **性能基线**
   - ✗ 1000 图标加载时间
   - ✗ 搜索响应时间 <100ms
   - ✗ 内存使用画像
