/**
 * E2E Smoke Tests — Placeholder
 *
 * These will use Playwright's Electron support after the Electron upgrade (Task 4).
 * Playwright requires Node >= 18, which isn't available until after we upgrade.
 *
 * Tests to implement:
 * - App window opens with correct dimensions
 * - Splash screen visible on launch
 * - Can create new project
 * - Can import SVG icons
 * - Can export icon fonts
 * - webSecurity is enabled (not false)
 */

describe('E2E Smoke Tests', () => {
  test.todo('app window opens with correct title');
  test.todo('splash screen is visible on launch');
  test.todo('app window has minimum dimensions');
  test.todo('can create a new project');
  test.todo('webSecurity is not disabled');
});
